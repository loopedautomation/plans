---
"looped-plans": patch
---

A mermaid diagram that fails to parse says so in its own figure only.
Mermaid had also been appending a full-size "Syntax error in text" picture
to the page, which landed under the status bar.
